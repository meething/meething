var self;
var med;

export default class Embed {
  constructor (mediator) {
    this.mediator = mediator;
    med = this.mediator;
    self = this;

    return this;
  }

  buildEmbed () {
    //dumbed down menu for embed, assumes a room.
    if(!med.room)
  }
}
